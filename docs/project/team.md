# Команда проекта

## Состав команды

Проект разработан группой из семи человек.

## Роли и вклад по этапам

### Начинова Мария

Роль: тимлид, бэкенд-лид, база данных.

Этап 1:
- развернула NestJS-бэкенд и PostgreSQL в Docker
- создала таблицы: roles, users, departments, document_types, document_categories, documents, document_routes
- написала seed.sql с тестовыми данными
- реализовала эндпоинты POST /auth/login и GET /dashboard/data
- настроила JWT-аутентификацию, bcrypt, валидацию DTO, CORS
- настроила логирование (московское время, пароли и токены скрыты)
- реализовала запоминание входа в логине (localStorage/sessionStorage)
- координировала команду, проверяла код, доработки всех модулей

Этап 2:
- создала модуль Documents: GET /documents (список с фильтрацией и пагинацией), GET /documents/:id (карточка)
- создала справочные эндпоинты: GET /document-types, GET /document-categories, GET /departments
- создала 4 новые таблицы: document_sources, document_files, ocr_results, document_classifications
- написала DTO для валидации и ответов
- обновила seed.sql (15+ записей в каждой таблице)
- координировала команду, проверяла код, доработки всех модулей

Этап 3:
- создала AI-модуль: POST /documents/:id/analyze-ai, GET /documents/:id/ai-result
- интегрировала DeepSeek API (промпт, парсинг JSON)
- реализовала шифрование API-ключа через AES-256-CBC
- добавила мок-режим (AI_MOCK_MODE)
- создала таблицы ai_settings и document_ai_results
- создала модуль Settings: GET/PUT /settings/ai, GET /settings/ai/providers
- реализовала поиск по документам (GET /documents/search)
- реализовала поиск по документам (фронт)
- добавила aiResult в карточку документа
- координировала команду, проверяла код, доработки всех модулей

Этап 4:
- реализовала загрузку файлов: POST /documents/upload (Multer, до 50 МБ)
- реализовала извлечение текста: POST /documents/:id/extract-text (pdf-parse, mammoth, xlsx, tesseract.js)
- настроила валидацию форматов (PDF, DOCX, TXT, XLSX, JPG, PNG, TIFF)
- реализовала авто-генерацию регистрационного номера (ВХ-2026-XXX)
- реализовала удаление документов: DELETE /documents/:id
- доработала карточку документа на фронтенде (кнопка удаления)
- координировала команду, проверяла код, доработки всех модулей

Этап 5:
- улучшила OCR: предобработка изображений через sharp (авто-поворот, бинаризация, нормализация)
- реализовала автозаполнение полей документа после AI-анализа
- реализовала эндпоинты: PUT /documents/:id/verify, POST /documents/:id/route, GET /documents/routing, PUT /document-routes/:id/status, PUT /documents/:id
- создала модуль профиля: GET/PUT /auth/profile, POST /auth/avatar, смена пароля
- создала настройки уведомлений: таблица user_notification_settings, эндпоинты GET/PUT /settings/notifications
- создала настройки интерфейса: таблица user_interface_settings, эндпоинты GET/PUT /settings/interface
- создала модуль безопасности: таблицы login_history, user_sessions, audit_log, эндпоинты сессий, истории входов, аудит-лога
- реализовала поиск по OCR-тексту
- реализовала экспорт в Excel (GET /documents/export)
- создала таблицу route_templates и эндпоинт GET /route-templates
- создала таблицу document_comments и эндпоинты GET/POST /documents/:id/comments
- создала таблицу audit_log и эндпоинт GET /settings/security/audit-log
- добавила сроки обработки (verified_at, routed_at в document.entity)
- создала страницу профиля на фронтенде
- доработала карточку документа (выпадающие списки, кнопки подтверждения и маршрутизации, комментарии)
- провела координацию команды, проверку кода, доработки всех модулей
- интегрировала иконки, валидацию справочников, уведомления для справочников
- полностью доработала тёмную тему
- исправила пагинацию, московское время, конфликты CSS-классов

Этап 6:
- написала полную документацию (архитектура, API, база данных, развёртывание, переменные окружения, логирование, безопасность)
- написала пользовательскую документацию (руководство оператора, руководство администратора, краткая памятка)
- написала отчётную документацию (пояснительная записка, отчёт о тестировании, план презентации)
- реализовала ролевую модель с разграничением доступа (RolesGuard, декоратор @Roles)
- создала админ-панель: AdminModule с 20 эндпоинтами, AdminPanel.tsx с 6 вкладками
- реализовала управление пользователями: создание, блокировка, сброс пароля, смена роли, удаление
- создала таблицу system_settings и эндпоинты для системных настроек
- реализовала ограничения загрузки из базы данных (размер, количество, форматы)
- реализовала полное и выборочное резервное копирование (экспорт/импорт)
- реализовала массовую рассылку уведомлений администратором
- добавила очистку журнала действий и системных данных
- добавила фильтрацию админских уведомлений (обязательные не отключаются)
- перевела все типы уведомлений и действий на русский язык
- скрыла административные функции у оператора на всех страницах
- обновила страницу загрузки для отображения актуальных настроек
- улучшила вкладку безопасности в настройках (фильтры, пагинация)
- провела финальную проверку и доработки всех страниц

### Москалева Александра

Роль: фронтенд-лид, бэкенд и фронтенд.

Этап 1:
- инициализировала React-приложение с TypeScript и Vite
- настроила роутинг (/login, /dashboard)
- создала общие компоненты: Card, Table, Header, Sidebar
- создала global.css с CSS-переменными (дизайн-система)
- сверстала общий Layout дашборда

Этап 2:
- создала эндпоинты справочников на бэкенде: GET /document-types, GET /document-categories, GET /departments
- добавила маршруты для новых страниц (/documents, /documents/:id)
- настроила ProtectedRoute для защиты страниц

Этап 3:
- создала модуль Settings на бэкенде: эндпоинты GET/PUT /settings/ai, GET /settings/ai/providers
- создала эндпоинт поиска GET /documents/search
- добавила роутинг для /dashboard/settings
- собрала архив из 20+ тестовых файлов

Этап 4:
- создала страницу загрузки: /dashboard/incoming (drag-and-drop, выбор файла)
- реализовала цепочку статусов: загрузка, извлечение текста, AI-анализ
- добавила кнопку сканирования в Header

Этап 5:
- переписала страницу уведомлений (Notifications.tsx, адаптив)
- создала Scanner.tsx (камера, захват кадра, загрузка)
- переписала страницу очереди проверки (Verification.tsx)
- доработала пакетную загрузку (до 15 файлов, прогресс-бар)
- добавила экспорт в Excel (кнопка в DocumentsListPage)

Этап 6:
- проверила и доработала: уведомления, карточка документа, Header (дропдаун уведомлений и профиля), поиск, настройки
- подготовила презентацию

### Мейсарош Карина

Роль: связка фронтенда и бэкенда.

Этап 1:
- создала services/api.ts с настройками Axios
- написала функции login() и getDashboard()

Этап 2:
- написала 5 API-функций: getDocuments, getDocumentById, getDocumentTypes, getDocumentCategories, getDepartments
- интегрировала API со страницами списка документов и карточки

Этап 3:
- написала 6 API-функций: getAiSettings, updateAiSettings, getAiProviders, analyzeDocument, getDocumentAiResult, searchDocuments
- интегрировала AI-анализ в карточку и поиск в Header

Этап 4:
- написала API-функции: uploadDocument, extractText
- добавила типы UploadResponse, ExtractTextResponse
- интегрировала со страницей загрузки

Этап 5:
- написала все API-функции для новых эндпоинтов: verifyDocument, routeDocument, getRoutingDocuments, updateRouteStatus, updateDocument, getProfile, updateProfile, uploadAvatar, настройки уведомлений и интерфейса, безопасность, комментарии, экспорт
- добавила все типы в types/index.ts
- переписала страницу подразделений (Departments.tsx, Departments.css)
- переписала страницу маршрутизации (Routing.tsx, Routing.css)

Этап 6:
- проверила и доработала: профиль, маршрутизация, страница входа

### Нехланова Алина

Роль: фронтенд.

Этап 1:
- сверстала страницу логина (/login)
- реализовала валидацию формы (email, пароль)

Этап 2:
- сверстала карточку документа (/documents/:id): общая информация, файлы, маршруты
- подключила API для получения данных
- реализовала состояния загрузки, ошибки, 404

Этап 3:
- добавила вкладку AI-анализ в карточку документа
- реализовала кнопку запуска анализа и отображение результата
- исследовала форматы файлов (PDF, DOCX, XLSX, TIFF, JPG, PNG, TXT)

Этап 4:
- доработала карточку документа (новый layout)
- выполнила адаптив страницы загрузки

Этап 5:
- переписала страницу аналитики (Analytics.tsx, Analytics.css, графики recharts)
- выполнила адаптив аналитики и маршрутизации
- помогала с адаптивом других страниц

Этап 6:
- проверила и доработала: аналитика, страница подразделений, сканер

### Ефанов Егор

Роль: фронтенд.

Этап 1:
- сверстал главную страницу дашборда
- встроил компоненты Card для метрик и Table для последних документов

Этап 2:
- сверстал страницу списка документов (/documents)
- реализовал фильтрацию (тип, категория, статус)
- подключил API для получения данных

Этап 3:
- реализовал поиск в Header с debounce и выпадающим меню
- создал страницу настроек (вкладка AI-провайдер)

Этап 5:
- создал вкладки в настройках: уведомления, интерфейс, безопасность
- реализовал тёмную тему (data-theme="dark")
- реализовал пагинацию с выбором количества записей

Этап 6:
- проверил и доработал: архив, подразделения (карточки отделов)
- полностью проверил тёмную тему на всех страницах и элементах

### Мотовилова Мария

Роль: фронтенд, адаптив.

Этап 2:
- выполнила адаптив страницы списка документов (таблица, фильтры)
- доделала адаптив дашборда

Этап 3:
- выполнила адаптив поиска в Header
- выполнила адаптив AI-блока в карточке документа

Этап 5:
- выполнила адаптив поиска
- выполнила адаптив страницы подразделений (Departments.css)

Этап 6:
- проверила и доработала: дашборд, сайдбар, загрузка

### Мельникова Виолетта

Роль: фронтенд, адаптив.

Этап 1:
- выполнила адаптив страницы логина

Этап 2:
- выполнила адаптив страницы карточки документа 
Этап 3:
- выполнила адаптив страницы настроек

Этап 5:
- выполнила адаптив страницы очереди проверки